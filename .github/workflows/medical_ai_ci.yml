name: Medical AI CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
        submodules: recursive  # <--- FIX 1: Downloads the frontend code
        lfs: true              # <--- FIX 2: Downloads the 110MB model weights

    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f backend/requirements.txt ]; then pip install -r backend/requirements.txt; fi

    - name: Check Code Quality (Linting)
      # This checks for syntax errors but ignores style warnings (E501)
      run: |
        pip install flake8
        # stop the build if there are Python syntax errors or undefined names
        flake8 backend --count --select=E9,F63,F7,F82 --show-source --statistics

    - name: Build Docker Images (Sanity Check)
      run: |
<<<<<<< Updated upstream
        docker compose build
=======
        docker compose build
>>>>>>> Stashed changes
